#include "banana.dtsi"
#include <dt-bindings/zmk/pointing.h>

&default_transform { // Offset of 5 because the left side has 5 columns
    col-offset = <5>;
};

&trackball_split {
    device = <&trackball>;
    status = "okay";
};